📘 PDF Rule Checker — Frontend

Live Project: https://pdf-rule-checker-frontend.vercel.app/

🚀 Setup Instructions
1. Clone the project
git clone https://github.com/Prince-Ranaa/PDF-Rule-Checker---frontend.git
cd PDF-Rule-Checker---frontend


(Or simply open the folder if already downloaded.)

2. Install dependencies
npm install

3. Create a .env.local file

Inside the frontend root folder:

NEXT_PUBLIC_API_DEV="http://localhost:5000"
This tells the frontend where your backend is running in local development.

4. Run the frontend
npm run dev


The app will start at:
👉 http://localhost:3000

⚠️ Important

To use the frontend correctly, make sure the backend server is running locally at:
http://localhost:5000
